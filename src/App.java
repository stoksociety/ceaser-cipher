import java.util.Scanner;// Import the Scanner class

public class App{
    public static void main(String[] args){

        Scanner scanner = new Scanner(string[] args){
        System.out.println("what do you want?" /n 1."Encode" /n 2."Decode" /n 3."Quit");

            scanner.NextInt = new scanner.NextInt();
            scanner.nextLine();
            if (choice ==1){
            System.out.println("Enter text ");
                String inputText;
                inputText = Scanner.nextLine().ToUppercase();
                System.out.println("Enter key");
            int key;
            key = Scanner.nextInt();
//            Encoding encoding= new Encoding(inputText,Key);
//            System.out.println(String.format("Your encrypted text is: %s",Decoding.decode()));
        }

        else if (choice ==2) {
                scanner.NextInt = new scanner.NextInt();
                scanner.nextLine();
//                if (choice ==1){
                    System.out.println("Enter text ");
                    String inputText;
                    inputText = Scanner.nextLine().ToUppercase();
                    System.out.println("Enter key");
                    int key;
                    key = Scanner.nextInt();
            }

        else if (choice == 3){
            System.exit (status 0);
        }
        else {
        System.out.println("no choice was selected");
        }
    }
}